find ./scripts \
  -path ./node_modules -prune -o \
  -path ./dist -prune -o \
  -name "*.js" -type f -print \
| sort | while read file; do
  echo "// ===================================="
  echo "// 📁 $file"
  echo "// ===================================="
  cat "$file"
  echo
done > projeto_completo.txt




stripcomments arquivo.js > arquivo-limpo.js
